<div class="col-md-4 mx-auto">
    <div class="card">
        <div class="card-header">
            <h3>Edita la Nota</h3>
        </div>
        <div class="card-body">
            <!-- Lo siguiente es una manera oculta de enviar un PUT a
            través de un POST
            utlizando el middleware methodOverride configurado con 
            -method. Oculto pero visible si se inspecciona el código-->
            <form action="/notes/edit-note/{{note._id}}?_method=PUT" method='POST'>
            <input type="hidden" name='_method' value='PUT'>
            <!---->
            <div class="form-group">
                <input type="text" name='title' class='form-comtrol' placeholder='Title' value='{{note.title}}'>
            </div>
            <div class="form-group">
                <textarea name="description" class='form-control'>
                    {{note.description}}
                </textarea>
            </div>
            <div class="form-group">
                <button class='btn btn-primary btn-block' type='submit'>
                    Confirmar
                </button>
            </div>
            </form>
        </div>
    </div>
</div>